# assets/

This folder contains lightweight **SVG** figures used in the main README.

- `banner.svg` — top banner
- `architecture.svg` — high-level service flow
- `demo.svg` — simple UI/mock screenshot

You can edit these SVGs in any vector editor (Figma, Inkscape) or directly in a text editor.
